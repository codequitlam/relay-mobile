import 'package:flutter/material.dart';
import 'screens/bt_search.dart';

class App extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
      home: Scaffold(
        appBar: BtSearch(),
        body: Container(),
      )
    );
  }
}